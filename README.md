# Khmer HipHip Rating Web Application

> ### Pre-requisites <br/>
> <li>Firebase SDK Credentials (from Google Firebase project settings)</li>

To get started, in the root directory, run 
`npm install`

Then to get the instance running
`npm start`
